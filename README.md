# ProcessMappingAgentFn

Agent lambda for automatic process mapping conversations.

It injects canvas context and target-user profile context into the agent prompt,
then relies on dynamically equipped tools to refine the canvas, record
contributions, and save the member profile.
